import React from 'react';
import styles from './about.module.scss'

const About = () => (
    <div className={styles.container}>
        <span>Hello, I'm on the about page!</span>
    </div>
);

export default About;
